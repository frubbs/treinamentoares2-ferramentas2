#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../stubby/bin/stubby" "$@"
  ret=$?
else 
  node  "$basedir/../stubby/bin/stubby" "$@"
  ret=$?
fi
exit $ret
